# Convert numeric time value (hour-based) to character hour in %H:%M format

Convert numeric time value (hour-based) to character hour in %H:%M
format

## Usage

``` r
convert_time_num_to_chr(time_num)
```

## Arguments

- time_num:

  Numeric time value (hour-based)
